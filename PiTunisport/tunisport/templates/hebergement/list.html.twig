{% extends 'back.html.twig' %}

{% block main %}
<br><br><br><br><br>
{{form(form_NSC)}}

<table border="1">
	<tr>
		<th>NSC</th>
		<th>Name</th>
        <th>Email</th>
		<th>Delete</th>
		<th>Show</th>
	</tr>
	{% for s in Hebergements %}
		<tr>
			<TD><img src="{{ asset('/uploads/images/' ~ s.getImage()) }}" width="100px" height="100px" alt="" /></TD>
			<td>{{s.id}}</td>
			<td>{{ s.nomheberg}}</td>
			<td>
				<button>
					<a href="{{ path ('delete_Hebergement', {'id': s.id})}}">delete</a>
				</button>
			</td>
			<td>
				<button>
					<a href="{{ path ('showS', {'id': s.id})}}">show</a>
				</button>
			</td>
			<td>
				<button>
					<a href="{{ path ('app_Hebergementedit', {'id': s.id})}}">update</a>
				</button>
			</td>
		</tr>
	{% endfor %}
</table>
<br>
<a href="{{path ('Add')}}"> Ajouter</a>
{% endblock %}